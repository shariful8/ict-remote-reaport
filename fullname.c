#include<stdio.h>
#include<string.h>
main()
{
    int i, len, total=0;
    char s[100]
    printf(" Enter the string");
    gets(s)
    len=string(s);
    for(i=0, i<len, i++)
    {
        if(s[i]=='a')(s[i]=='A')(s[i]=='e')(s[i]=='e')(s[i]=='E')(s[i]=='i')(s[i]=='I')(s[i]=='o')(s[i]=='O')
        {
            total++;
        }
    }
    printf("total %d vowels are theere", total);
    }
